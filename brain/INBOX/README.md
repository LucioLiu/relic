<!--
  收件箱 / Inbox
  导入缓冲区：把躯壳的旧记忆文件放在这里，让躯壳自动转换。
  Import buffer: place agent's old memory files here for auto-conversion.
-->

# 收件箱 / Inbox

> 📭 **目录现在是空的，这是正常的。**
> 只有当你需要从其他 AI Agent 导入旧记忆时才用得到这里。把旧记忆文件放进来，告诉 AI 转换就行。

然后告诉躯壳："请阅读 INBOX/ 里的文件，按 PROTOCOL.md 的格式转换到 MEMORY.md"
Then tell the agent: "Read files in INBOX/, convert to MEMORY.md per PROTOCOL.md"

用户确认导入无误后，此目录中的源文件会被移到 ARCHIVE/raw/ 永久保存。
Source files will be moved to ARCHIVE/raw/ for permanent storage after user confirms import is correct.
