# SESSIONS/ 对话记录 / Session Logs

按月份建文件夹（YYYY-MM），每天一个文件——上面只是示例。
Organize by month (YYYY-MM), one file per day — the examples above are just samples.

原始对话，不改不摘要，永不删除。
Raw conversations — never modified, never summarized, never deleted.
